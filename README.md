# primo-progetto
ciao questo è il mio primo progetto su git hub
DOMANDE
2)E' un tag che simboleggia il fatto che stiamo parlando di una pagina web
4) il tag "<p>" serve per andare a capo con una frase ma anche semplicemente con una parola o lettera
3) Il tag h1 ti fa inserire un testo che è in grassetto e maggiore a livello di grandezza di h2 che sarà a sua volta più grande di h3
5) la riga "a href" serve per importare qualcosa all'interno dellla pagian web 
6) Visualizzi lo sfondo in arancione e quello scritto in h2, la relazione è con il file .css che ti permette di cambiare il colore dello sfond con: body{ background; orange} 7) Perchè nel file style.css abbiamo detto che il colore di background del body deve essere arancione. 8) Il tag serve per collegare un file CSS esterno alla pagina HTML, così può avere colori, font e stili grafici
10) Il selettore in un file CSS serve per scegliere a quali elementi HTML applicare uno stile
 11) Per far comparire il testo "hello" sulla console bisogna inserire il testo nel file java script scrivendo console.log("") e tra le virgolette i testo che si vuole inserire 12) Per scrivere sulla console ciao al posto di hello ho inserito "Ciao" invece che "Hello" nelle virgolette della parentesi di console.log 
 13) Si posso vedere la scitta "Ciao" nella console 
 14) Compare un popup con la scritta "Ciao"
  15) La differenza tra console.log() e alert() è che consoloe.log viene mostrato nella console del browser mentre alert è un popup sullo schermo 16) Il file script.js serve per scrivere il codice JavaScript che fa funzionare una pagina web. 17) Per collegare un file script.js a un file HTML, bisogna scrivere alla fine del file .html questo: <script src="script.js"></script> 
 18) I file Html servono per creare la struttura e il contenuto di una pagina web, come testi, immagini, link e titoli. 19) Per modificare la parte grafica del progetto come colori, posizioni e grandezze elementi come il bordo etc.
  20) I file .js servono per far fare cose alla pagina web, tipo muovere i bottoni o far comparire messaggi.